# Motion Mosaic 使用说明

## 你会收到什么

只需要一个 `MotionMosaic.exe`。把它放到一个固定文件夹里，例如桌面上的 `MotionMosaic` 文件夹，然后双击打开。

第一次打开时如果 Windows 提示安全确认，请只在确认文件来源可信时继续运行。

## 基本流程

1. 打开 `MotionMosaic.exe`。
2. 在左侧 `图片目录` 点 `选择`，选择装有图片、GIF 或 WebP 的文件夹。
3. 选择要处理的文件。
4. 用 `矩形` 或 `多边形` 工具在画面上框出需要打码的区域。
5. 动图可以用上一帧、下一帧逐帧检查和补打码。
6. 处理完一张后点 `标为完成`。
7. 全部处理完后点 `导出已完成`。
8. 导出文件会保存到 `导出目录`，默认是 exe 旁边的 `exports` 文件夹。

## 动图批量应用

1. 在一帧上画好或选中打码区域。
2. 拖动上方时间轴选择目标帧范围；要应用到整段动图时，直接点 `全片`。
3. 点 `把本帧区域应用到目标帧`。这一次点击会立即执行，不需要再点确认或应用。

如需从目标范围撤回这一组区域，点 `从目标帧移除这些区域`。

## 查看与缩放

- 图片默认会完整适应中间画面，不会出现滚动条。
- 需要看细节时，按住 `Ctrl` 并滚动鼠标滚轮放大或还原。
- 拖动上方时间轴时，中间画面会同步切换到对应帧。

## 导出格式

- 静态图片会导出为 WebP。
- 动图默认导出为 AVIF，也可以在设置里改成 WebP 或 WebM。
- 默认会自动压缩到设置里的目标大小。
- 导出完成后，左侧会直接显示最终成品预览，可检查真实打码效果。

## 常见问题

### 双击后打开了浏览器

这是正常兜底模式。工具仍然在本机运行，直接在浏览器页面里使用即可。

### 导出很慢

AVIF 压缩质量好但比较慢。需要速度优先时，在设置里把动图格式改成 WebP 或 WebM。

### 找不到导出的文件

看左侧 `导出目录`。如果没有改过，导出结果在 `MotionMosaic.exe` 同级的 `exports` 文件夹。

### 可以关掉黑色窗口吗

正常打包后的版本不会弹出导出压缩黑窗口。如果看到旧版本弹窗，请换最新的 `MotionMosaic.exe`。
